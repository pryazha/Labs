#include "Dialog.cpp"

int main() {
	// Person *A = new Person;
	// A->Input();
	// Abiturient *B = new Abiturient;
	// B->Input();

	// List list(3);
	// list.Show();
	// Object* p = A;
	// list.Push_back(p);
	// p = B;
	// list.Push_back(p);
	// list.Show();
	// list.Pop_back();
	// list.Show();

	Dialog D;
	D.Execute();

	return 0;
}